'use client';import {useState} from 'react';export function WidgetOrder({initial}:{initial:string[]}){const [items,setItems]=useState(initial);return <div className='space-y-2'>{items.map((item,i)=><div key={item} className='flex gap-3 items-center border border-white/10 p-3 rounded-lg'><input type='hidden' name='widget_order' value={item}/><span className='flex-1'>{item}</span><button type='button' aria-label={'Subir '+item} disabled={!i} onClick={()=>{const next=[...items];[next[i-1],next[i]]=[next[i],next[i-1]];setItems(next);}}>↑</button><button type='button' aria-label={'Descer '+item} disabled={i===items.length-1} onClick={()=>{const next=[...items];[next[i+1],next[i]]=[next[i],next[i+1]];setItems(next);}}>↓</button></div>)}</div>;}