import {requireAdmin} from '@/lib/auth/session';import {Card} from '@/components/ui/card';import {ActionForm} from '@/components/action-form';import {createOrganization,updateOrganization} from '../actions';export default async function Page(){const {db}=await requireAdmin();const {data}=await db.from('organizations').select('*').eq('is_agency',false).order('name');return <div className='space-y-6'><h1 className='text-3xl font-semibold'>Clientes</h1><Card><h2 className='text-lg mb-5'>Criar workspace</h2><ActionForm action={createOrganization} label='Criar cliente'><div className='field-grid'><label>Nome do cliente<input name='name' required/></label><label>Endereço do dashboard<input name='slug' required pattern='[a-z0-9]+(-[a-z0-9]+)*' placeholder='nome-do-cliente'/></label></div></ActionForm></Card>{data?.map(o=><Card key={o.id}><ActionForm action={updateOrganization}><input type='hidden' name='id' value={o.id}/><div className='field-grid'><label>Nome<input name='name' defaultValue={o.name} required/></label><label>Status<select name='status' defaultValue={o.status}><option value='active'>Ativo</option><option value='paused'>Pausado</option></select></label></div></ActionForm></Card>)}</div>;}
