'use server';import {z} from 'zod';import {revalidatePath} from 'next/cache';import {requireAdmin} from '@/lib/auth/session';import type {ActionState} from '@/components/action-form';
export async function createOrganization(_:ActionState,f:FormData):Promise<ActionState>{const {db}=await requireAdmin();const input=z.object({name:z.string().trim().min(2).max(160),slug:z.string().regex(/^[a-z0-9]+(-[a-z0-9]+)*$/)}).safeParse({name:f.get('name'),slug:f.get('slug')});if(!input.success)return {ok:false,message:'Informe nome e endereço válido (letras minúsculas e hífens).'};if(['admin','login','api','auth','_next'].includes(input.data.slug))return {ok:false,message:'Este endereço é reservado.'};const {error}=await db.from('organizations').insert(input.data);revalidatePath('/admin');return {ok:!error,message:error?'Não foi possível criar. Verifique se o endereço já existe.':'Cliente criado com sucesso.'};}
export async function updateOrganization(_:ActionState,f:FormData):Promise<ActionState>{const {db}=await requireAdmin();const id=z.string().uuid().parse(f.get('id'));const parsed=z.object({name:z.string().trim().min(2),status:z.enum(['active','paused'])}).safeParse({name:f.get('name'),status:f.get('status')});if(!parsed.success)return {ok:false,message:'Dados inválidos.'};const {error}=await db.from('organizations').update(parsed.data).eq('id',id).eq('is_agency',false);revalidatePath('/admin');return {ok:!error,message:error?'Não foi possível salvar.':'Cliente atualizado.'};}
export async function saveMember(_:ActionState,f:FormData):Promise<ActionState>{const {db}=await requireAdmin();const input=z.object({organization_id:z.string().uuid(),user_id:z.string().uuid(),role:z.enum(['client_admin','editor','viewer']),is_active:z.boolean()}).safeParse({organization_id:f.get('organization_id'),user_id:f.get('user_id'),role:f.get('role'),is_active:f.get('is_active')==='on'});if(!input.success)return {ok:false,message:'Preencha organização, UUID do usuário e role.'};const {data:existing}=await db.from('organization_members').select('role').eq('organization_id',input.data.organization_id).eq('user_id',input.data.user_id).maybeSingle();if(existing?.role==='super_admin')return {ok:false,message:'O acesso de super administrador não pode ser alterado neste formulário.'};const {error}=await db.from('organization_members').upsert(input.data);revalidatePath('/admin/users');return {ok:!error,message:error?'Usuário não encontrado no Auth ou vínculo inválido.':'Acesso atualizado.'};}
