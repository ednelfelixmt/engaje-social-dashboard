import {requireAdmin} from '@/lib/auth/session';import {ActionForm} from '@/components/action-form';import {Card} from '@/components/ui/card';import {saveMember} from '../actions';export default async function Page(){const {db}=await requireAdmin();const [{data:orgs},{data:members}]=await Promise.all([db.from('organizations').select('id,name'),db.from('organization_members').select('*')]);return <div className='space-y-6'><h1 className='text-3xl font-semibold'>Usuários e acessos</h1><Card><ActionForm action={saveMember}><div className='field-grid'><label>Organização<select name='organization_id'>{orgs?.map(o=><option key={o.id} value={o.id}>{o.name}</option>)}</select></label><label>UUID do usuário Supabase Auth<input name='user_id' required/></label><label>Permissão<select name='role'><option value='viewer'>Visualizador</option><option value='editor'>Editor</option><option value='client_admin'>Administrador do cliente</option></select></label></div><label className='!flex-row'><input type='checkbox' name='is_active' defaultChecked/>Acesso ativo</label></ActionForm></Card><Card className='overflow-auto'><table><thead><tr><th>Usuário</th><th>Organização</th><th>Role</th><th>Status</th></tr></thead><tbody>{members?.map(m=><tr key={m.user_id+m.organization_id}><td>{m.user_id}</td><td>{orgs?.find(o=>o.id===m.organization_id)?.name}</td><td>{m.role}</td><td>{m.is_active?'Ativo':'Inativo'}</td></tr>)}</tbody></table></Card></div>;}
