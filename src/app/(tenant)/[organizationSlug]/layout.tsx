import {tenant} from '@/lib/auth/session';import {Navigation} from '@/components/navigation';import type {CSSProperties} from 'react';export default async function Layout({children,params}:{children:React.ReactNode;params:{organizationSlug:string}}){const {db,org}=await tenant(params.organizationSlug);const [{data:branding},{data:config}]=await Promise.all([db.from('branding').select('*').eq('organization_id',org.id).single(),db.from('dashboard_configs').select('*').eq('organization_id',org.id).single()]);const image=branding?.dashboard_background_path?(await db.storage.from('branding').createSignedUrl(branding.dashboard_background_path,3600)).data?.signedUrl:null;return <div style={{'--primary':branding?.primary_color,'--background':branding?.background_color} as CSSProperties}><Navigation slug={org.slug} enabled={config?.enabled_pages}/><main className='lg:ml-60 min-h-screen p-6 lg:p-9' style={{background:image?`linear-gradient(#0f0f13ed,#0f0f13ed),url("${image}") center/cover`:undefined}}>{children}</main></div>;}